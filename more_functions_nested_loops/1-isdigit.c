#include "main.h"

/**
 * _isdigit - function that checks for a digit (0 through 9).
 * @c: integer
 * Return: 1 (digit) or 0
 */

int _isdigit(int c)
{


	{
		return (1);
	}

	else
	{
		return (0);
	}

}
